<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.useful.todolist.user.mapper.UserMapper">

    <insert id="saveAll">
        INSERT INTO user (user_id, user_name, password, role_id, group_id)
        VALUES
        <foreach collection="users" item="user" separator=",">
            (#{user.userId}, #{user.userName}, #{user.password}, #{user.roleId}, #{user.groupId})
        </foreach>
    </insert>

</mapper>
